<!DOCTYPE html>
<html lang="en"  data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/main.css">
    <title>BLOG</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@500&family=Noto+Serif:ital,wdth,wght@1,87.5,500&display=swap" rel="stylesheet">

</head>
<body>
    <div class="container">
        <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="text-center mb-4">
              <a class="main blog-header-logo text-body-emphasis text-decoration-none fs-1 fw-bold" href="#">My Personal Corner</a>
            </div>
        </div>
        <a href="/blog/new" class="cent btn btn-outline-light btn-lg mb-4">Start Writing...</a>
        <% articles.forEach(article => { %>
            <div class="p-4 p-md-5 mb-4 rounded text-body-emphasis bg-body-secondary w-50">
                <div class="col-lg-6 px-0">
                    <h1 class="display-4 fst-italic fw-bold"><%= article.title %></h1>
                    <p class="lead my-3"><%= article.date.toLocaleDateString() %></p>
                    <p class="lead mb-0"><a href="blog/<%= article.slug %>" class="text-body-emphasis fw-bold">Read Blog</a></p>
                </div>
                
                    
                    <form action="/blog/<%= article.id %>?_method=DELETE"
                        method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
              </div>
       <%  }); %>

    </div>
</body>
</html>